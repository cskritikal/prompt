Perform ALL steps in order:

STEP 1: TRIAGE
- analyze alert
- enrich
- choose route

STEP 2: BUILD OUTPUT
- produce artifact in strict format

STEP 3: VALIDATE FORMAT
- check for:
  - single code block
  - no extra sections
  - exact template

IF INVALID:
- REWRITE output into correct format

STEP 4: QA CHECK
- verify:
  - routing correctness
  - severity correctness
  - suppression safety

IF ANY ISSUE FOUND:
- MODIFY output to correct it

OUTPUT ONLY FINAL VERSION
